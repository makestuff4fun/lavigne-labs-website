# Contact form email delivery (optional for local dev).
# Without this, the contact form still works — submissions are logged to the
# terminal instead of emailed. Set it when you're ready to receive real emails.
#
# Get a key at https://resend.com and verify the lavignelabs.com domain.
RESEND_API_KEY=
